
if command -v dockerd >/dev/null; then
    if ! ps -ef | grep dockerd | grep -q -v grep; then
        nohup bash -c '
        sudo dockerd \
        --host=unix:///var/run/docker.sock \
        --storage-driver=overlay2 >/dev/null 2>&1' >/dev/null 2>&1 &
    fi
fi